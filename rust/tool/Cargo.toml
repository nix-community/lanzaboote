[workspace]
resolver = "2"

members = [
    "shared",
    "systemd",
]

default-members = [
    "shared",
    "systemd"
]

[workspace.package]
version = "0.5.0"
edition = "2024"

[profile.release]
opt-level = "s"
lto = true
