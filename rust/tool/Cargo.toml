[workspace]

members = [
    "shared",
    "systemd",
]

default-members = [
    "shared",
    "systemd"
]

[workspace.package]
version = "0.3.0"
edition = "2021"

[profile.release]
opt-level = "s"
lto = true
